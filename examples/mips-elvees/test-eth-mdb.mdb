source mem_nvcom02t.cfg 
loadelf test-eth-mdb.elf

#
# Переменные для управления тестом
#
# set start_delay    <миллисек>   -   задержка после инициализации
#     контроллера и физ. уровня для того, чтобы контроллер смог 
#     установить связь; по умолчанию 3000.
#
# set local_loop     <0, 1>       -   включать ли петлю в МAC;
#     по умолчанию 1 (включать).
#
# set packet_size   <число байт>  -   длина передаваемого пакета;
#     по умолчанию 1500 байт.
#
# set nb_of_packets <число пакетов> - число передаваемых пакетов
#     по умолчанию 1000 пакетов.
#
#
# Отображение результата
# 
# show nb_of_received       -    количество принятых пакетов.
#
# show nerr                 -    количество ошибок, интегральный 
#     признак; если равен 0, то тест пройден, иначе нет.
#     Тест проверяет, что количество переданных пакетов равно 
#     количеству принятых, а также, что содержимое отправленных
#     и принятых пакетов одинаково.
#

#set start_delay     5000
set local_loop      1
set packet_size     1500
set nb_of_packets   100
run
show nb_of_received
show nerr
